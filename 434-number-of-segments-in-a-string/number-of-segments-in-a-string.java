class Solution {
    public int countSegments(String s) {
        String arr[]=s.trim().split("\\s+");
    if(s.trim().equals("")){
        return 0;
    }
    int count=arr.length;
    
      return count;
    }
}